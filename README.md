# Clustering-model-on-text-data
•	Build a clustering model on text data. The model should take one column which will be text format , convert the text into vectors using embedding and build Clustering model. 
